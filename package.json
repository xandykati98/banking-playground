{
  "name": "banking-playground",
  "private": true,
  "workspaces": ["agent_server"],
  "scripts": {
    "dev": "npm run dev --workspace=agent_server",
    "dev:flutter": "cd banking_app && flutter run -d chrome",
    "dev:server": "npm run dev --workspace=agent_server"
  }
}
